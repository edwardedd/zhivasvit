# $document.on 'click', '.scheme-container .link', ()->

#   terminal_id = $(this).attr("data-terminal")
#   window.document.location = terminal_id

  # window.document.location = $(this).data("href")